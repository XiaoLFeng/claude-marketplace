# Memory MCP 工具完整参考指南

嘿嘿~ 这里是所有 Memory 工具的详细文档！(´∀`)💖

## 概述

Memory 是 llm-memory MCP 系统中用于持久化存储重要信息的核心功能。通过作用域系统，支持私有、小组和全局三个级别的知识管理。

### 作用域系统详解

```
personal  → 当前目录专属（私有）
            - 数据只在当前路径可见
            - 切换目录后看不到
            - 适合项目特定的内容

group     → 组内所有路径共享
            - 需要先使用 group_add_path 加入组
            - 组内的所有路径都能访问
            - 适合多仓库协作场景

global    → 全局可见（仅 Memory 支持！）
            - 在任何路径都能访问
            - 跨项目共享
            - 适合通用知识和最佳实践

【作用域选择建议】
✅ 使用 global=true：
   - 通用知识和最佳实践
   - 跨项目共享的配置
   - 用户全局偏好

✅ 使用 scope="personal"：
   - 项目特定的记录
   - 临时笔记和想法
   - 本地实验数据

✅ 使用 scope="group"：
   - 团队协作项目
   - 多仓库管理
   - 共享的开发环境
```

---

## Memory 工具详解（6个）

### 1. `memory_list` - 列出记忆

**用途**：查看所有可用的记忆

```javascript
memory_list({
  scope: "personal" | "group" | "global" | "all"  // 可选，默认 "all"
})
```

**参数说明**：
- `scope` (可选)
  - `"personal"`: 仅列出当前路径的私有记忆
  - `"group"`: 仅列出所在组的记忆
  - `"global"`: 仅列出全局记忆
  - `"all"`: 列出所有可见的记忆（默认）

**返回格式**：
```javascript
{
  success: true,
  data: [
    {
      code: "mem-api-design",
      title: "API 设计规范",
      category: "项目规范",
      scope_label: "[私有]",  // [私有] / [小组] / [全局]
      priority: 2,
      tags: ["api", "design", "guidelines"],
      created_at: "2024-12-05T10:30:00Z",
      updated_at: "2024-12-05T15:45:00Z"
    },
    // ... 更多记忆
  ]
}
```

**使用示例**：
```javascript
// 查看所有可见的记忆
memory_list({ scope: "all" })

// 仅查看私有记忆
memory_list({ scope: "personal" })

// 查看全局知识库
memory_list({ scope: "global" })
```

**错误处理**：
```javascript
{
  success: false,
  error: "无权限访问此作用域"
}
```

---

### 2. `memory_create` - 创建记忆

**用途**：创建新的知识记录

```javascript
memory_create({
  code: "mem-xxx",                // 必填，全局唯一
  title: "标题",                  // 必填
  content: "内容",                // 必填
  category: "分类",               // 可选，默认"默认"
  tags: ["tag1", "tag2"],         // 可选
  priority: 2,                    // 可选，1-4，默认2
  global: false,                  // 可选，true=全局可见
  scope: "personal"               // 可选: personal, group
})
```

**参数详解**：

1. **code** (必填)
   - 格式：全小写字母，可包含连字符
   - 长度：最少 3 个字符
   - 规则：开头和末尾必须是字母，不能含下划线
   - 示例：`mem-api-design`, `mem-jwt-decision`
   - ❌ 错误：`mem_design`, `Mem-Design`, `-mem-design`

2. **title** (必填)
   - 知识的标题
   - 建议简明扼要（20-50字）
   - 示例：`"JWT vs Session 选型分析"`

3. **content** (必填)
   - 知识的详细内容
   - 支持 Markdown 格式
   - 建议结构化编写：背景 → 核心内容 → 示例 → 参考

4. **category** (可选)
   - 知识分类
   - 默认值：`"默认"`
   - 推荐分类：
     - `"架构决策"` - 技术选型、设计模式
     - `"问题排查"` - Bug 分析、解决方案
     - `"代码示例"` - 常用代码片段
     - `"项目规范"` - 命名规范、编码规范
     - `"数据库设计"` - Schema 设计、迁移
     - `"项目总结"` - 项目回顾、经验总结

5. **tags** (可选)
   - 标签数组，便于搜索
   - 建议 3-5 个标签
   - 示例：`["auth", "jwt", "security"]`

6. **priority** (可选)
   - 优先级（1-4）
   - 默认值：`2`
   - `1`：低 - 可选内容
   - `2`：中 - 常规内容（默认）
   - `3`：高 - 重要知识
   - `4`：紧急 - 关键决策

7. **global** (可选)
   - 是否全局可见
   - 默认值：`false`
   - `true`：全局可见（推荐通用知识）
   - `false`：根据 scope 限制访问

8. **scope** (可选)
   - 作用域：`"personal"` 或 `"group"`
   - 默认值：`"personal"`
   - 仅在 global=false 时有效

**返回格式**：
```javascript
{
  success: true,
  data: {
    code: "mem-jwt-decision",
    title: "JWT vs Session 选型分析",
    created_at: "2024-12-05T10:30:00Z"
  }
}
```

**完整示例**：
```javascript
memory_create({
  code: "mem-jwt-decision",
  title: "JWT vs Session 选型分析",
  content: `# 技术选型分析

## 为什么选择 JWT

### 优点
1. **无状态**: 服务器无需存储会话
2. **可扩展**: 易于水平扩展

### 安全措施
- 强制 HTTPS 传输
- Refresh Token 使用 HttpOnly Cookie
- Access Token 存储在内存中

## 参考资料
- [JWT Handbook](https://auth0.com/blog/jwt-handbook/)`,
  category: "架构决策",
  tags: ["auth", "jwt", "decision"],
  priority: 3,
  global: false,
  scope: "personal"
})
```

**错误处理**：
```javascript
// Code 格式错误
{
  success: false,
  error: "code 格式错误: 全小写字母，可含连字符，开头末尾必须是字母"
}

// Code 已存在
{
  success: false,
  error: "code 全局唯一冲突: 记录已存在"
}

// 权限问题
{
  success: false,
  error: "无权限操作此作用域"
}
```

---

### 3. `memory_get` - 获取记忆详情

**用途**：获取单个记忆的完整信息

```javascript
memory_get({
  code: "mem-xxx"  // 必填
})
```

**参数说明**：
- `code` (必填)：要查询的记忆 code

**返回格式**：
```javascript
{
  success: true,
  data: {
    code: "mem-jwt-decision",
    title: "JWT vs Session 选型分析",
    content: "# 技术选型分析\n\n...",
    category: "架构决策",
    tags: ["auth", "jwt", "decision"],
    priority: 3,
    scope: "personal",
    scope_label: "[私有]",
    is_global: false,
    created_at: "2024-12-05T10:30:00Z",
    updated_at: "2024-12-05T15:45:00Z",
    created_by: "user",
    word_count: 543
  }
}
```

**返回字段说明**：
- `code`: 记忆标识
- `title`: 记忆标题
- `content`: 完整内容（Markdown）
- `category`: 分类
- `tags`: 标签列表
- `priority`: 优先级
- `scope`: 作用域（personal/group/global）
- `scope_label`: 作用域标签，便于显示
- `is_global`: 是否全局可见
- `created_at`: 创建时间
- `updated_at`: 最后更新时间
- `created_by`: 创建者
- `word_count`: 内容字数

**使用示例**：
```javascript
// 查看详细内容
const result = memory_get({ code: "mem-jwt-decision" })

if (result.success) {
  console.log(result.data.content)
}
```

**错误处理**：
```javascript
// 记录不存在
{
  success: false,
  error: "记录不存在或无权限访问"
}

// Code 拼写错误
{
  success: false,
  error: "未找到 code 为 'mem-typo' 的记录"
}
```

---

### 4. `memory_update` - 更新记忆

**用途**：修改已有记忆的内容

```javascript
memory_update({
  code: "mem-xxx",           // 必填
  title: "新标题",           // 可选
  content: "新内容",         // 可选
  category: "新分类",        // 可选
  tags: ["new", "tags"],     // 可选
  priority: 3                // 可选
})
```

**参数详解**：

1. **code** (必填)
   - 要更新的记忆 code

2. **title** (可选)
   - 新的标题
   - 不提供则保持原值

3. **content** (可选)
   - 新的内容
   - 支持 Markdown
   - 不提供则保持原值

4. **category** (可选)
   - 新的分类
   - 不提供则保持原值

5. **tags** (可选)
   - 新的标签列表
   - 完全替换原标签（不是追加）
   - 不提供则保持原值

6. **priority** (可选)
   - 新的优先级（1-4）
   - 不提供则保持原值

**返回格式**：
```javascript
{
  success: true,
  data: {
    code: "mem-jwt-decision",
    updated_at: "2024-12-05T16:00:00Z"
  }
}
```

**使用示例**：
```javascript
// 仅更新内容
memory_update({
  code: "mem-jwt-decision",
  content: "# 更新的技术选型分析\n\n..."
})

// 同时更新多个字段
memory_update({
  code: "mem-jwt-decision",
  title: "JWT vs Session 决策 v2.0",
  priority: 3,
  tags: ["auth", "jwt", "security", "best-practice"]
})

// 提升优先级
memory_update({
  code: "mem-jwt-decision",
  priority: 4  // 从 3 改为 4
})
```

**错误处理**：
```javascript
// 记录不存在
{
  success: false,
  error: "记录不存在或无权限访问"
}

// 无权限编辑
{
  success: false,
  error: "无权限编辑此记录"
}

// 无任何更新字段
{
  success: false,
  error: "至少需要提供一个要更新的字段"
}
```

---

### 5. `memory_delete` - 删除记忆

**用途**：删除不需要的记忆记录

```javascript
memory_delete({
  code: "mem-xxx"  // 必填
})
```

**参数说明**：
- `code` (必填)：要删除的记忆 code

**返回格式**：
```javascript
{
  success: true,
  data: {
    code: "mem-jwt-decision",
    deleted_at: "2024-12-05T16:10:00Z"
  }
}
```

**使用示例**：
```javascript
// 删除过时的记忆
memory_delete({ code: "mem-old-design" })

// 删除临时笔记
memory_delete({ code: "mem-temp-notes" })
```

**重要注意**：
```
⚠️ 删除是不可恢复的操作！
   - 删除后无法恢复
   - 建议先用 memory_get 确认内容
   - 如果是重要知识，考虑记录备份

✅ 安全删除的做法：
1. memory_get({ code: "mem-xxx" })  // 先查看
2. 确认无需保留后再执行 delete
3. 检查返回结果确认删除成功
```

**错误处理**：
```javascript
// 记录不存在
{
  success: false,
  error: "记录不存在或无权限访问"
}

// 无权限删除
{
  success: false,
  error: "无权限删除此记录"
}
```

---

### 6. `memory_search` - 搜索记忆

**用途**：按关键词搜索记忆

```javascript
memory_search({
  keyword: "关键词",         // 必填
  scope: "all"               // 可选: personal, group, global, all
})
```

**参数详解**：

1. **keyword** (必填)
   - 搜索关键词
   - 支持模糊搜索
   - 在 title 和 content 中搜索
   - 不区分大小写
   - 示例：`"JWT"`, `"认证"`, `"database"`

2. **scope** (可选)
   - `"personal"`: 仅搜索私有记忆
   - `"group"`: 仅搜索组内记忆
   - `"global"`: 仅搜索全局记忆
   - `"all"`: 搜索所有可见记忆（默认）

**返回格式**：
```javascript
{
  success: true,
  data: {
    total: 3,
    results: [
      {
        code: "mem-jwt-decision",
        title: "JWT vs Session 选型分析",
        category: "架构决策",
        scope_label: "[私有]",
        priority: 3,
        tags: ["auth", "jwt"],
        snippet: "...采用 JWT 机制来实现无状态认证...",
        match_type: "title_and_content"  // title / content / both
      },
      // ... 更多结果
    ]
  }
}
```

**返回字段说明**：
- `total`: 找到的记忆总数
- `results`: 搜索结果列表
  - `code`: 记忆标识
  - `title`: 记忆标题
  - `category`: 分类
  - `scope_label`: 作用域标签
  - `priority`: 优先级
  - `tags`: 标签列表
  - `snippet`: 内容摘要（包含上下文）
  - `match_type`: 匹配位置（title / content / both）

**使用示例**：
```javascript
// 搜索与 JWT 相关的记忆
memory_search({ keyword: "JWT" })

// 仅搜索私有记忆
memory_search({
  keyword: "认证",
  scope: "personal"
})

// 搜索全局知识库
memory_search({
  keyword: "最佳实践",
  scope: "global"
})

// 搜索多个关键词（返回包含任意一个的结果）
memory_search({ keyword: "登录" })  // 自动包含相关词
```

**错误处理**：
```javascript
// 关键词为空
{
  success: false,
  error: "keyword 不能为空"
}

// 无权限搜索
{
  success: false,
  error: "无权限搜索此作用域"
}

// 搜索无结果
{
  success: true,
  data: {
    total: 0,
    results: []
  }
}
```

---

## 快速参考

### 优先级对照表

```
Priority 4 - 紧急 🔴
✅ 关键架构决策
✅ 安全漏洞解决方案
✅ 生产问题排查记录

Priority 3 - 高 🟠
✅ 重要技术选型
✅ 核心代码示例
✅ 关键特性实现

Priority 2 - 中 🟡（默认）
✅ 常规最佳实践
✅ 标准实现方案
✅ 一般项目规范

Priority 1 - 低 🟢
✅ 可选的优化建议
✅ 学习资料链接
✅ 备选方案记录
```

### Code 命名规范

```
推荐命名模式：
mem-<主题>

有效示例：
✅ mem-api-design
✅ mem-jwt-decision
✅ mem-user-preferences
✅ mem-deployment-guide

无效示例：
❌ mem_design        (含下划线)
❌ Mem-Design        (含大写)
❌ -mem-design       (开头不是字母)
❌ mem-design-       (末尾不是字母)
❌ md                (少于3字符)
```

### 常见操作组合

```javascript
// 场景 1: 记录新的架构决策
memory_create({
  code: "mem-xxx",
  title: "...",
  content: "...",
  category: "架构决策",
  tags: [...],
  priority: 3,
  global: false
})

// 场景 2: 记录通用最佳实践
memory_create({
  code: "mem-best-practice-xxx",
  title: "...",
  content: "...",
  category: "项目规范",
  global: true  // 全局可见
})

// 场景 3: 搜索相关知识
memory_search({
  keyword: "相关词",
  scope: "personal"
})

// 场景 4: 更新过时的记忆
memory_update({
  code: "mem-xxx",
  content: "更新的内容",
  priority: 2
})

// 场景 5: 清理不需要的记录
memory_delete({ code: "mem-xxx" })
```

---

呀~ 这就是所有 Memory 工具的详细参考！希望对你有帮助~ (´∀`)💖
