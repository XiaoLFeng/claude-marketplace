# Plan Mode Context 工作流参考

## 完整工作流

```
进入 Plan 模式
    ↓
检测到 "Plan mode is active" 提示
    ↓
┌─────────────────────────────────┐
│         并行加载数据            │
├─────────────────────────────────┤
│ plan_list()  →  未完成计划     │
│ todo_list()  →  待办任务       │
│ memory_search() → 相关记忆     │
└─────────────────────────────────┘
    ↓
对重要项调用 get 获取详情
    ↓
汇总格式化输出
    ↓
开始规划任务
```

## MCP 工具详解

### plan_list

```javascript
plan_list({
  scope: "all"  // personal/group/all
})

// 返回
[
  {
    code: "plan-xxx",
    title: "计划标题",
    progress: 45,
    status: "in_progress"
  }
]
```

### plan_get

```javascript
plan_get({
  code: "plan-xxx"
})

// 返回完整计划内容
{
  code: "plan-xxx",
  title: "计划标题",
  description: "计划描述",
  content: "详细内容...",
  progress: 45
}
```

### todo_list

```javascript
todo_list({
  scope: "all"
})

// 返回
[
  {
    code: "todo-xxx",
    title: "任务标题",
    priority: 3,
    status: 0  // 0=pending, 1=in_progress, 2=completed
  }
]
```

### memory_search

```javascript
memory_search({
  keyword: "关键词",
  scope: "all"
})

// 返回匹配的记忆列表
```

### memory_get

```javascript
memory_get({
  code: "mem-xxx"
})

// 返回完整记忆内容
```

## 优先级展示

| 优先级 | 图标 | 含义 |
|-------|------|------|
| 4 | 🔴 | 紧急 |
| 3 | 🟠 | 高 |
| 2 | 🟡 | 中（默认） |
| 1 | 🟢 | 低 |

## 最佳实践

1. **并行加载**：同时调用多个 list，提高效率
2. **按需获取详情**：只对重要项调用 get
3. **简洁展示**：不要输出过多信息，突出重点
4. **关联分析**：找出计划和任务之间的关联
