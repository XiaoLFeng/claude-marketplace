---
name: session-init
description: "新对话初始化器 - 新对话开始时自动加载当前项目状态。快速获取进行中的计划和待办，让 Claude 了解项目背景，避免重复询问。"
---

# Session Init

新对话开始时自动加载项目状态，快速了解当前进展。

## 触发条件

- 新对话开始
- 用户说"当前状态"、"有什么进展"、"项目情况"
- 用户切换到新项目目录

## 操作流程

### Step 1: 快速概览

```javascript
// 并行获取状态概览
const [plans, todos] = await Promise.all([
  plan_list({ scope: "all" }),
  todo_list({ scope: "all" })
]);

// 过滤活跃项
const activePlans = plans.filter(p => p.progress < 100);
const pendingTodos = todos.filter(t => t.status !== 2);
```

### Step 2: 简洁输出

```
📊 项目状态概览

📋 计划: ${activePlans.length} 个进行中
✅ 待办: ${pendingTodos.length} 个待完成
  - 🔴 紧急: ${urgentCount} 个
  - 🟠 高优先: ${highCount} 个
```

## 与 plan-mode-context 的区别

| 特性 | session-init | plan-mode-context |
|------|-------------|-------------------|
| 触发时机 | 新对话开始 | 进入 Plan 模式 |
| 输出详细度 | 简洁概览 | 详细信息 |
| 是否获取详情 | 否 | 是 |
| 目的 | 快速了解 | 辅助规划 |

## MCP 工具使用

- `plan_list` - 列出计划
- `todo_list` - 列出待办

详见：[工作流参考](./references/workflow.md)
